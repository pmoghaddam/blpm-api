'use strict';

var helper = require('../testHelper');

describe('Basic (skeleton) test', function () {

    /**
     * Tests
     */
    it('should pass', function () {
        assert.ok(helper);
    });
});
